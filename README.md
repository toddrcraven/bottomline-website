# BottomLine ERP Marketing Site

Marketing site for BottomLine. Built with Next.js.

## Development

- Install dependencies with `npm install`.
- Run the local dev server with `npm run dev`.

## Deployment

- Production builds via `npm run build`.
- Start the build output with `npm run start`.
